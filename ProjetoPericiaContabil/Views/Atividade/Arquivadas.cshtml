@model List<ProjetoPericiaContabil.Models.Atividade>

@{
    ViewBag.Title = "Atividades Arquivadas";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="page-header">
    <div>
        <h1 class="page-title">Atividades Arquivadas</h1>
        <p class="page-subtitle">Atividades conclu&iacute;das e j&aacute; visualizadas pelo cliente.</p>
    </div>
    <div class="page-actions">
        <a class="btn btn-secondary" href="@Url.Action("MinhasFuncionario", "Atividade")">
            <i class="bi bi-arrow-left"></i> Voltar
        </a>
    </div>
</div>

<section class="card table-card">
    <div class="table-responsive">
        <table class="table app-table">
            <thead>
                <tr>
                    <th scope="col">T&iacute;tulo</th>
                    <th scope="col">Descri&ccedil;&atilde;o</th>
                    <th scope="col">Resultado</th>
                    <th scope="col">Arquivos</th>
                    <th scope="col">Status</th>
                    <th scope="col">A&ccedil;&atilde;o</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td><strong>@item.Titulo</strong></td>
                        <td><div class="descricao">@item.Descricao</div></td>
                        <td><div class="resultado">@item.Resultado</div></td>
                        <td>
                            @if (!string.IsNullOrEmpty(item.ArquivoClienteCaminho))
                            {
                                <a class="arquivo-link d-block" href="@Url.Action("BaixarArquivoCliente", "Atividade", new { id = item.Id })" aria-label="Baixar arquivo do cliente da atividade @item.Titulo">
                                    <i class="bi bi-paperclip"></i> Arquivo do cliente
                                </a>
                            }
                            @if (!string.IsNullOrEmpty(item.ArquivoResultadoCaminho))
                            {
                                <a class="arquivo-link d-block" href="@Url.Action("BaixarArquivoResultado", "Atividade", new { id = item.Id })" aria-label="Baixar arquivo do resultado da atividade @item.Titulo">
                                    <i class="bi bi-file-earmark-check"></i> Arquivo do resultado
                                </a>
                            }
                            else if (string.IsNullOrEmpty(item.ArquivoClienteCaminho))
                            {
                                <span class="arquivo-vazio">Sem arquivos</span>
                            }
                        </td>
                        <td><span class="status-badge status-arquivada">Visualizada pelo cliente</span></td>
                        <td>
                            <a class="btn btn-secondary btn-sm" href="@Url.Action("Details", "Atividade", new { id = item.Id })" aria-label="Ver detalhes da atividade @item.Titulo">
                                <i class="bi bi-eye"></i> Detalhes
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</section>
